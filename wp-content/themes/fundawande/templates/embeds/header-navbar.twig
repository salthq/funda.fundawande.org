<!-- ******************* The Navbar Area ******************* -->
<div  id="wrapper-navbar"  class="wrapper-fluid wrapper-navbar " >
    <nav class="navbar navbar-expand-lg d-flex align-items-center h-100">

            <a id="main-menu-button" class="nav-link " href="#"  role="button" >
                <div class="nav-hamburger nav-hamburger--htx  align-self-center ml-0 "><span></span></div>
                <span class="open-text ml-2 text-uppercase">open menu</span>
                <span class="close-text ml-2 text-uppercase">close menu</span>
            </a>
            {#<!-- TODO: create context for user role in functions.timber.php and add logic for assigning role in class-fundawande-login.php-->#}
            {#{% if user_role == 'learner' %}#}
                {#<div class="dropdown-menu" aria-labelledby="navbarDropdown">#}
                    {#{% for item in learner_menu.get_items %}#}
                        {#<a class="dropdown-item" href="{{ item.link }}"><i class = "{{ item.class }}"></i> {{ item.title }}</a>#}
                    {#{% endfor %}#}
                {#</div>#}
            {#{% elseif user_role == 'coach'%}#}
                {#<div class="dropdown-menu" aria-labelledby="navbarDropdown">#}
                    {#{% for item in coach_menu.get_items %}#}
                        {#<a class="dropdown-item" href="{{ item.link }}"><i class = "{{ item.class }}"></i> {{ item.title }}</a>#}
                    {#{% endfor %}#}
                {#</div>#}
            {#<!-- catch-all in case a user isn't a coach or a learner -->    #}
            {#{% else %}#}
                {#<div class="dropdown-menu" aria-labelledby="navbarDropdown">#}
                    {#{% for item in learner_menu.get_items %}#}
                        {#<a class="dropdown-item" href="{{ item.link }}"><i class = "{{ item.class }}"></i> {{ item.title }}</a>#}
                    {#{% endfor %}#}
                {#</div>#}
            {#{% endif %}#}



        <!--TODO: add JS scripts and data-target for viewing information modals-->
        <a class=" > <i class = "fas fa-question-circle"></i> View Tooltips</a>


        <div>
            <div class = "btn-group" role = "group" aria-label = "language buttons">
                <form method="GET">
                    <button class="btn btn-info language-button" type="submit" name="lang" value="eng">Change Language</button>
                </form>
                <form method="GET">
                    <button class="btn btn-info language-button" type="submit" name="lang" value="xho">Tshintsha ulwimi</button>
                </form>
            </div>
        </div>
    </nav>
</div><!-- .wrapper-navbar end -->